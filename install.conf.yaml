- shell:
  - [git submodule update --init --recursive, Installing submodules]

- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.vimrc: vimrc
    ~/.oh-my-zsh: ohmyzsh
    ~/.zshrc: zshrc
    ~/.p10k.zsh: p10k.zsh
    ~/.zsh-custom: zsh-custom
